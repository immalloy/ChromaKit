# Contributing

Thanks for improving ChromaKit. Keep changes focused, test them locally, and update the README when behavior or setup changes.

## Local Checks

From the repository root:

```bat
python -m compileall src
```

For runtime testing on Windows:

```bat
requirements.bat
run.bat
```

For executable builds:

```bat
build.bat
```
